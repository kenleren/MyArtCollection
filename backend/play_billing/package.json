{
  "name": "@archivale/play-billing",
  "version": "0.0.0",
  "private": true,
  "main": "build/src/index.js",
  "description": "Isolated Archivale Play subscription verification backend.",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test build/dist-test/*.test.js",
    "test:emulator": "npm run build && node --test build/emulator-test/*.test.js"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "firebase": "^12.11.0",
    "typescript": "^5.8.3"
  }
}
