{
  "name": "@archivale/research-broker",
  "version": "0.0.0",
  "private": true,
  "main": "build/src/index.js",
  "description": "Archivale research broker scaffold with fake-provider tests and a disabled-by-default OpenAI shell.",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test build/dist-test/*.test.js",
    "test:emulator": "firebase emulators:exec --project demo-art-research-broker --only auth,functions,firestore --config firebase.emulator.json \"npm run build && node build/dist-test/emulator_harness.js\""
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
